# ScreenScout Vanilla JS

A vanilla JavaScript movie site built from the same two-page flow and DOM-rendering style as the old `user-posts` project, but adapted to match the ScreenScout React app.

## Pages
- `index.html` — search, sort, watchlist filter, recent searches, result cards
- `movie.html` — detailed title page

## Features
- OMDb search with API key `87fd279a`
- localStorage-powered watchlist
- recent searches
- sorting and watchlist-only filtering
- click-through details page

## Run
Open `index.html` with Live Server for the smoothest experience.
# javascript-
